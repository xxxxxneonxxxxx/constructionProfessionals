{
  "name": "tok-lead-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var LOG_NOTIFICATIONS:true",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/validation.js && node --check src/security.js && node --check src/notifications.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
